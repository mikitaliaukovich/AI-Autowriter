# Runtime — install into .venv with Python 3.12 (see scripts/setup.ps1).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27
numpy>=1.26

# Voice activity detection (Silero VAD, ONNX, CPU — ~1 MB model)
onnxruntime>=1.19

# Speech recognition. CTranslate2 has no ROCm backend, so this runs int8 on the CPU
# while the GPU stays dedicated to the LLM.
faster-whisper>=1.1
